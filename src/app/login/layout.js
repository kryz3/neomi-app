export const metadata = {
  title: "Connexion Admin - Neomi",
  description: "Page de connexion pour l'administration Neomi",
};

export default function LoginLayout({ children }) {
  return children;
}
